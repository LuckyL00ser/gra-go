#pragma once
namespace ResourceIDs {

	enum TextureID { /**<Enum reprezentuje identyfikatory tekstur*/
		menuBackground,
		boardBackground,
		menuList,
		settingsBackground,
		button,
		whiteStone,
		blackStone,
		whosTurn,
		wait
	};

	enum FontID {  /**<Enum reprezentuje identyfikatory czcionek (w tej aplikacji mało wykorzystywane - jedna czcionka)*/
		mainFont,
		additionalFont
	};


}
